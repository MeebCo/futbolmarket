version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # Set VIEW_ONLY for staging and main (Amplify sets AWS_BRANCH)
        # Override in Amplify Console env vars if needed
        - |
          if [ "$AWS_BRANCH" = "staging" ] || [ "$AWS_BRANCH" = "main" ]; then
            export NEXT_PUBLIC_VIEW_ONLY=true
          fi
          npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
